# v2.9.3 Security & Commercial Readiness Blockers QA

## Purpose

Verify all critical and high-priority security/commercial-readiness blockers are fixed before v3.0.0 release.

---

## Critical Issue Checklist #1–#9

### #1 Unauthenticated Image Access
- [ ] `GET /api/image?key=...` returns 401 without session cookie
- [ ] Authenticated OEM user can access images for their tenant defects
- [ ] Authenticated supplier user can access images for their assigned defects
- [ ] Cross-tenant image key returns 403 (no leak of file existence)
- [ ] Cache-Control is `private` (not `public`)

### #2 Backend Feature Gates on Detail Pages (PPAP)
- [ ] Free OEM user visiting `/quality/oem/ppap/[id]` is redirected
- [ ] Pro OEM user can access PPAP detail page
- [ ] Supplier user cannot access OEM PPAP detail

### #3 Backend Feature Gates on Detail Pages (FMEA)
- [ ] Free OEM user visiting `/quality/oem/fmea/[id]` is redirected
- [ ] Pro OEM user can access FMEA detail page
- [ ] Supplier user cannot access OEM FMEA detail

### #4 Backend Feature Gates on Server Actions
- [ ] PPAP server actions call `requireFeature("PPAP")` — verified already present
- [ ] IQC server actions call `requireFeature("IQC")` — verified already present
- [ ] FMEA server actions call `requireFeature("FMEA")` — verified already present
- [ ] Supplier Development server actions call `requireFeature("SUPPLIER_DEVELOPMENT")` — verified already present
- [ ] Executive Cockpit data loader calls `requireFeature("EXECUTIVE_COCKPIT")` — verified already present
- [ ] Quality Intelligence data loader calls `requireFeature("QUALITY_INTELLIGENCE")` — verified already present
- [ ] Supplier Scorecard data loader calls `requireFeature("SUPPLIER_SCORECARD")` — verified already present

### #5 Backend Feature Gates on API Routes
- [ ] `/api/fmea/save` calls `requireFeature("FMEA")` — verified
- [ ] `/api/ai/*` routes call `requireFeature("AI_CLASSIFICATION" | "FMEA" | "EIGHT_D")` — verified
- [ ] All AI routes restrict to OEM companyType — verified

### #6 Free-Tier AI Access
- [ ] Free OEM user cannot trigger AI Classification via `/api/ai/suggest` — returns 403
- [ ] Free OEM user cannot trigger AI 8D Review — returns error
- [ ] Free OEM user cannot trigger Root Cause Suggestion — returns error
- [ ] Free OEM user cannot trigger Similar Issues — returns error
- [ ] Supplier users cannot access OEM-only AI routes — returns 403
- [ ] `AI_ENABLED=false` returns safe error message, no raw provider errors

### #7 Cross-Tenant Supplier Assignment — PPAP
- [ ] OEM cannot assign supplier from another tenant via `createPpapRequest`
- [ ] OEM can only assign suppliers with existing records relationship
- [ ] Error message is clear: "Supplier is not associated with your company"

### #8 Cross-Tenant Supplier Assignment — All Modules
- [ ] `createIqcInspection` validates OEM-supplier association via `assertSupplierBelongsToOem`
- [ ] `createFmea` validates OEM-supplier association via `assertSupplierBelongsToOem`
- [ ] `createFieldDefect` validates OEM-supplier association via `assertSupplierBelongsToOem`
- [ ] `assignSupplier` (field defect) validates OEM-supplier association via `assertSupplierBelongsToOem`
- [ ] `createDevPlan` validates OEM-supplier association — already had `isAssociated` check, now uses shared helper
- [ ] FMEA new page (`/quality/oem/fmea/new`) only shows OEM-associated suppliers
- [ ] PPAP new page uses `getSuppliers()` (OEM-scoped) — verified
- [ ] IQC new page uses `getSuppliers()` (OEM-scoped) — verified

### #9 Unscoped Cron Queries
- [ ] `CRON_SECRET` env var is checked on `/api/cron/sla-reminders`
- [ ] Returns 401 without valid `CRON_SECRET`
- [ ] Returns 401 with wrong `CRON_SECRET`
- [ ] Notification creation scopes `companyId` per tenant record (oemId or supplierId)
- [ ] No cross-tenant notification delivery
- [ ] Cron does not accept client-provided `companyId` to operate broadly

---

## High-Priority Checklist #10–#13

### #10 Middleware Protection Gaps
- [ ] `src/proxy.ts` (Next.js 16 proxy) exists and matches protected routes
- [ ] Unauthenticated request to `/quality/oem/*` redirects to `/login`
- [ ] Unauthenticated request to `/quality/supplier/*` redirects to `/login`
- [ ] Unauthenticated request to `/oem/*` redirects to `/login`
- [ ] Unauthenticated request to `/api/defects/*` returns 401
- [ ] Unauthenticated request to `/api/field/*` returns 401
- [ ] Unauthenticated request to `/api/image?key=...` returns 401
- [ ] Unauthenticated request to `/api/ai/*` returns 401
- [ ] Unauthenticated request to `/api/upload` returns 401
- [ ] Public routes (`/login`, `/verify-request`, `/api/auth/*`, `/api/cron`) are not blocked
- [ ] No redirect loop on authenticated pages

### #11 Sidebar Gate Mismatch
- [ ] Free OEM sidebar shows locked icons for PPAP, IQC, FMEA, Escalation, War Room, Intelligence, Executive, Scorecard, Supplier Development
- [ ] Pro OEM sidebar shows unlocked PPAP, IQC, FMEA
- [ ] Enterprise OEM sidebar shows all unlocked
- [ ] Supplier sidebar shows unlocked Defects, Field Quality (always available)
- [ ] Supplier sidebar shows PPAP/IQC/FMEA with gate property (evaluates to allowed for suppliers with `supplierAccess: true`)
- [ ] Supplier sidebar does not show Intelligence, Executive Cockpit, Scorecard, War Room
- [ ] Locked links have `opacity-50` and lock icon

### #12 Redirect/Canonical Route Access
- [ ] `/quality/oem/settings/plan` redirects to `/oem/settings/plan`
- [ ] `/oem/settings/plan` requires OEM Admin auth — verified
- [ ] No open redirect vulnerabilities in redirect destinations
- [ ] Redirect destination page enforces same auth as canonical

### #13 Direct URL and Server Action Invocation
- [ ] Free OEM accessing `/quality/oem/executive` sees upgrade prompt (page-level gate)
- [ ] Free OEM accessing `/quality/oem/scorecard` sees upgrade prompt (page-level gate)
- [ ] Free OEM accessing `/quality/oem/supplier-development` sees upgrade prompt (page-level gate)
- [ ] Free OEM accessing `/quality/oem/quality-intelligence` sees upgrade prompt (page-level gate)
- [ ] Free OEM accessing `/quality/oem/ppap/[id]` is redirected (backend gate added)
- [ ] Free OEM accessing `/quality/oem/fmea/[id]` is redirected (backend gate added)
- [ ] Supplier user cannot access OEM-only pages (Executive, Intelligence, Scorecard)
- [ ] Unauthorized Server Actions return error, not exception

---

## Authenticated File/Image Access Checks

| Route | Auth | Tenant Scope | Feature Gate |
|---|---|---|---|
| `/api/image` | Session + companyId | Resolves parent record, checks oemId/supplierId | N/A (general) |
| `/api/defects/evidence/[id]` | Session + companyId | `canUserAccessDefectEvidence` | N/A |
| `/api/defects/evidence` (POST) | Session + companyId | `canUserAccessDefectEvidence` write | N/A |
| `/api/field/attachments/[...id]` | Session + companyId | Field defect oemId/supplierId | N/A |
| `/api/field/attachments` (POST) | Session + OEM role | `oemId` validation | N/A |
| `/api/upload` | Session + companyId | Key includes companyId | N/A |
| `/api/ppap/upload` | Session + companyId | PPAP supplierId match | `PPAP` |

---

## Backend Feature Gate Summary

| Module | Page Gate | Server Action Gate | API Route Gate |
|---|---|---|---|
| Executive Cockpit | `requireFeature` on page | In data loader | N/A |
| Quality Intelligence | `requireFeature` on page | In action | N/A |
| Supplier Scorecard | `requireFeature` on page | In data loader | N/A |
| Supplier Development | `requireFeature` on page | In actions | N/A |
| PPAP | `requireFeature` on list + NEW + **detail** (fixed) | In actions | In upload route |
| IQC | `requireFeature` on list + detail + NEW | In actions | N/A |
| FMEA | `requireFeature` on list + NEW + **detail** (fixed) | In actions | In save route |
| AI Classification | N/A (no page) | In ai-actions | In all AI routes |
| AI 8D Review | N/A (no page) | In ai-review actions | N/A |
| Root Cause Suggestion | N/A (no page) | In ai-review actions | N/A |
| War Room | `requireFeature` on page | N/A | N/A |
| Escalations | `requireFeature` on page | In field actions | N/A |
| Quality Linkage | Component-level | In manual-links | N/A |

---

## Free-Tier AI Access Checks

| AI Feature | Feature Gate | OEM Only | Company Scope |
|---|---|---|---|
| AI Classification | `AI_CLASSIFICATION` (PRO) | Yes | `oemId` |
| Similar Issues | `SIMILAR_ISSUES` (PRO) | Yes | `oemId` (DB-only) |
| AI 8D Review | `AI_8D_REVIEW` (ENTERPRISE) | Yes | `oemId` |
| Root Cause Suggestion | `ROOT_CAUSE_SUGGESTION` (ENTERPRISE) | Yes | `oemId` |
| AI FMEA Suggest | `FMEA` (PRO) | Yes | `oemId`/`supplierId` |
| AI Refine Text | `EIGHT_D` (FREE) | No | `companyId` |
| AI Suggest Diagram | `AI_CLASSIFICATION` (PRO) | Yes | `companyId` |
| AI Vision | `AI_CLASSIFICATION` (PRO) | Yes | — |

---

## Supplier Assignment Tenant Scope Summary

| Action | Before | After |
|---|---|---|
| `createPpapRequest` | `type: "SUPPLIER"` only | `assertSupplierBelongsToOem` |
| `createIqcInspection` | `type: "SUPPLIER"` only | `assertSupplierBelongsToOem` |
| `createFmea` | `type: "SUPPLIER"` only | `assertSupplierBelongsToOem` |
| `createFieldDefect` | `type: "SUPPLIER"` only | `assertSupplierBelongsToOem` |
| `assignSupplier` (field defect) | `type: "SUPPLIER"` only | `assertSupplierBelongsToOem` |
| `createDevPlan` | `isAssociated` query (adequate) | Shared helper |
| FMEA new page suppliers | ALL suppliers | OEM-associated only |

---

## Cron Scope Summary

| Job | Auth | Tenant Scope | Cross-Tenant Risk |
|---|---|---|---|
| SLA Reminders | `CRON_SECRET` header | Per-defect oemId/supplierId notification scoping | None — notifications scoped by companyId |

---

## Middleware/Redirect Summary

| Path | Proxy Check | Page Auth | Notes |
|---|---|---|---|
| `/quality/oem/*` | Session check via proxy | Each page calls `auth()` + `companyId` check | Defense-in-depth |
| `/quality/supplier/*` | Session check via proxy | Each page calls `auth()` + `companyType` check | Defense-in-depth |
| `/oem/*` | Session check via proxy | Plan page requires OEM Admin | Defense-in-depth |
| `/api/*` | Session check via proxy | Each route calls `auth()` | Returns 401 if no session |
| `/api/cron/*` | Skipped in proxy | `CRON_SECRET` header | Not user-facing |
| `/login`, `/verify-request` | Authenticated users redirected away | N/A | Not protected |

---

## Direct URL Access Matrix

| Module | Free OEM | Pro OEM | Enterprise OEM | Supplier |
|---|---|---|---|---|
| `/quality/oem/executive` | Upgrade prompt | Upgrade prompt | Full access | Redirected |
| `/quality/oem/quality-intelligence` | Upgrade prompt | Full access | Full access | Redirected |
| `/quality/oem/scorecard` | Upgrade prompt | Upgrade prompt | Full access | Redirected |
| `/quality/oem/supplier-development` | Upgrade prompt | Upgrade prompt | Full access | Redirected |
| `/quality/oem/ppap/[id]` | Redirect (fixed) | Full access | Full access | Redirected |
| `/quality/oem/iqc/[id]` | Redirect | Full access | Full access | Redirected |
| `/quality/oem/fmea/[id]` | Redirect (fixed) | Full access | Full access | Redirected |

---

## Server Action Unauthorized Invocation Matrix

| Server Action | Auth Check | Feature Gate | Tenant Scope |
|---|---|---|---|
| `createPpapRequest` | OEM + role + companyId | PPAP | `assertSupplierBelongsToOem` (fixed) |
| `createIqcInspection` | OEM + role + companyId | IQC | `assertSupplierBelongsToOem` (fixed) |
| `createFmea` | OEM + role + companyId | FMEA | `assertSupplierBelongsToOem` (fixed) |
| `createFieldDefect` | OEM + role + companyId | N/A (FREE) | `assertSupplierBelongsToOem` (fixed) |
| `assignSupplier` | OEM + role + companyId | N/A | `assertSupplierBelongsToOem` (fixed) |
| `generateClassification` | OEM + role | AI_CLASSIFICATION | `oemId` |
| `generateSimilarIssues` | OEM + role | SIMILAR_ISSUES | `oemId` |
| `generateAi8dReview` | OEM + role | AI_8D_REVIEW | `oemId` |
| `generateRootCauseSuggestion` | OEM + role | ROOT_CAUSE_SUGGESTION | `oemId` |
| `createDevPlan` | OEM + role | SUPPLIER_DEVELOPMENT | `isAssociated` + `oemId` |
| `createManualQualityLink` | OEM + role | QUALITY_LINKAGE | `verifyRecordBelongsToCompany` |
| `getIntelligenceData` | OEM + companyId | QUALITY_INTELLIGENCE | `oemId` |

---

## Regression Checklist

- [ ] Supplier Development list loads for eligible OEM
- [ ] Supplier Development plan create works with valid supplier
- [ ] Supplier Development plan create rejects unassociated supplier
- [ ] Supplier Scorecard list loads for Enterprise OEM
- [ ] Scorecard drill-down filtering works
- [ ] Executive Cockpit loads for Enterprise OEM
- [ ] Quality Intelligence loads for Pro+ OEM
- [ ] Quality Linkage manual link create/remove works
- [ ] FMEA create form shows only OEM-associated suppliers (fixed)
- [ ] FMEA detail page loads for Pro+ OEM (fixed gate)
- [ ] FMEA detail redirected for Free OEM accessing direct URL
- [ ] IQC create works with valid supplier
- [ ] IQC create rejects unassociated supplier (fixed)
- [ ] PPAP create works with valid supplier
- [ ] PPAP create rejects unassociated supplier (fixed)
- [ ] PPAP detail page loads for Pro+ OEM (fixed gate)
- [ ] PPAP detail redirected for Free OEM accessing direct URL
- [ ] Field Quality create with supplier validates association (fixed)
- [ ] Field Quality assign supplier validates association (fixed)
- [ ] AI Classification only for eligible users
- [ ] AI 8D Review only for Enterprise OEM
- [ ] Upgrade Request workflow still works
- [ ] Plan gating works
- [ ] Evidence upload/download still works
- [ ] Image access requires authentication (fixed)
- [ ] Middleware redirects unauthenticated users to login

---

## Docker Validation Checklist

- [ ] `docker-compose up -d --build app` succeeds
- [ ] App container starts and is running
- [ ] No fatal runtime errors in app logs
- [ ] App reachable at http://localhost:3000
- [ ] Login works
- [ ] Dashboard loads after login
- [ ] Image route requires auth

---

## Remaining Deferred Items

- AI Vision route could validate that imageUrl S3 key belongs to user's company (SSRF-like mitigations) — low risk since already behind auth/role/plan
- Per-route middleware authorization (beyond session cookie) — individual pages already enforce full auth
- API rate limiting — not in scope for this security patch
- Comprehensive audit logging of unauthorized access attempts — deferred to v3.0.0