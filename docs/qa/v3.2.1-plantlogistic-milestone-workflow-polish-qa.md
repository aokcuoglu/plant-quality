# PlantX v3.2.1 — PlantLogistic Production Milestone UX + Workflow Bugfix QA Checklist

## 1. Purpose

Validate that production milestone workflow polish, terminal state behavior, BLOCKED/Quality Hold resume consistency, default milestone idempotency, date validation, UI polish, module entitlement regression, and supplier denial are correct after v3.2.1 patch.

## 2. Test Personas

| Persona | Company | Role | Module Access |
|---------|---------|------|--------------|
| OEM Admin (Enterprise) | Enterprise Motors Group | ADMIN | PLANT_QUALITY_MODULE + PLANT_LOGISTIC_MODULE |
| OEM QE (Enterprise) | Enterprise Motors Group | QUALITY_ENGINEER | PLANT_QUALITY_MODULE + PLANT_LOGISTIC_MODULE |
| OEM Admin (PRO) | PlantX Automotive | ADMIN | PLANT_QUALITY_MODULE + PLANT_LOGISTIC_MODULE |
| OEM Admin (FREE) | TestFree OEM Corp | ADMIN | PLANT_QUALITY_MODULE only |
| Supplier Admin | Precision Parts Inc. | ADMIN | PLANT_QUALITY_MODULE only |

## 3. Terminal State Edit Checks

- [ ] COMPLETED milestone: updating title, description, plannedStart, plannedFinish, responsibleDepartment, delayReason returns error "Cannot edit workflow fields on a terminal milestone"
- [ ] COMPLETED milestone: updating notes succeeds
- [ ] CANCELLED milestone: same behavior as COMPLETED (workflow fields blocked, notes allowed)
- [ ] SKIPPED milestone: same behavior as COMPLETED (workflow fields blocked, notes allowed)
- [ ] Non-terminal milestone: all fields editable as before

## 4. BLOCKED / Quality Hold Resume Checks

- [ ] BLOCKED milestone with delayReason → transition to IN_PROGRESS → delayReason cleared, qualityHold cleared
- [ ] QUALITY_HOLD milestone with delayReason → transition to IN_PROGRESS → delayReason cleared, qualityHold cleared
- [ ] Both resume paths produce same behavior (consistent cleanup)
- [ ] BLOCKED → IN_PROGRESS transition creates timeline event
- [ ] QUALITY_HOLD → IN_PROGRESS transition creates timeline event

## 5. CANCELLED / SKIPPED Timeline Event Checks

- [ ] NOT_STARTED → CANCELLED transition creates STATUS_CHANGED timeline event
- [ ] PLANNED → CANCELLED transition creates STATUS_CHANGED timeline event
- [ ] SKIPPED transition creates STATUS_CHANGED timeline event (if SKIPPED becomes available in future)
- [ ] Timeline event message includes milestone title and new status

## 6. Default Milestone Idempotency Checks

- [ ] Clicking "Create Default Milestones" on order with no milestones creates 9 gates
- [ ] Clicking again after creation returns error "All default milestones already exist"
- [ ] Deleting one milestone then clicking "Create Default Milestones" re-creates only the missing gate
- [ ] No duplicate milestones created across repeated button clicks
- [ ] Timeline event message reflects actual number of created gates

## 7. Form Date Validation Checks

- [ ] Creating milestone with plannedStart after plannedFinish returns error
- [ ] Updating milestone with plannedStart after plannedFinish returns error
- [ ] Valid date ranges accepted without error
- [ ] Null/empty dates accepted (optional fields)

## 8. Production Progress Calculation Checks

- [ ] All COMPLETED milestones → 100% progress
- [ ] Mix of COMPLETED + SKIPPED → 100% progress (SKIPPED counts as resolved)
- [ ] Mix of COMPLETED + CANCELLED → less than 100% (CANCELLED does not count)
- [ ] Progress bar renders correctly for all percentages
- [ ] No NaN or negative progress values

## 9. Current Gate Display Checks

- [ ] Order with active IN_PROGRESS milestone → shows current gate name
- [ ] Order with BLOCKED milestone → shows blocked gate as current
- [ ] Order with QUALITY_HOLD milestone → shows held gate as current
- [ ] All milestones COMPLETED/SKIPPED → shows "All milestones completed" (detail) / "Completed" (list)
- [ ] All milestones COMPLETED/CANCELLED (no active) → shows "—" (no resolved completion claim)
- [ ] Order with no milestones → shows "—"

## 10. MilestoneGateBadge Label Check

- [ ] Gate badges show human-readable labels (e.g., "Body / Body Shop" not "BODY")
- [ ] All gate types display correctly: BODY, PAINT, ASSEMBLY, ELECTRICAL, POWERTRAIN, EOL_TEST, PDI, FINAL_QUALITY, YARD_READY, OTHER

## 11. Refresh / Revalidation Checks

- [ ] After default milestone create → page refreshes without manual reload
- [ ] After milestone status change → page refreshes without manual reload
- [ ] After milestone edit → page refreshes without manual reload
- [ ] Dashboard, order list, and order detail all update consistently

## 12. AppSwitcher / Module Entitlement Regression

- [ ] Enterprise OEM sees PlantLogistic as Live in AppSwitcher
- [ ] PRO OEM sees PlantLogistic as Live in AppSwitcher
- [ ] FREE OEM sees PlantLogistic as Locked in AppSwitcher
- [ ] Supplier does NOT see PlantLogistic in AppSwitcher
- [ ] PlantQuality shows as Live/Active for entitled users
- [ ] Active/Live/Locked/Soon badge differentiation correct for all modules
- [ ] Navigate to `/logistic` as FREE OEM → redirected to `/quality/oem`
- [ ] Navigate to `/logistic` as Supplier → redirected to `/quality/supplier`

## 13. Supplier Denial Checks

- [ ] Supplier cannot access `/logistic` (proxy redirect)
- [ ] Supplier cannot invoke `seedDefaultMilestonesForOrder`
- [ ] Supplier cannot invoke `changeProductionMilestoneStatus`
- [ ] Supplier cannot invoke `updateProductionMilestone`
- [ ] Supplier cannot invoke `deleteProductionMilestone`
- [ ] Supplier cannot invoke `createProductionMilestone`

## 14. PlantQuality Regression Checks

- [ ] `/quality/oem/defects` loads correctly
- [ ] `/quality/oem/field` loads correctly
- [ ] `/quality/oem/ppap` loads correctly
- [ ] `/quality/oem/iqc` loads correctly
- [ ] `/quality/oem/fmea` loads correctly
- [ ] `/quality/oem/scorecard` loads correctly
- [ ] `/quality/oem/supplier-development` loads correctly
- [ ] `/quality/oem/quality-intelligence` loads correctly
- [ ] No new TypeScript errors
- [ ] No new ESLint errors

## 15. Security / Tenant Isolation Checks

- [ ] All milestone server actions require authentication
- [ ] All milestone server actions require OEM companyType
- [ ] All milestone server actions require PLANT_LOGISTIC_MODULE
- [ ] All milestone server actions require PLANT_LOGISTIC feature gate
- [ ] All Prisma queries scoped by session.user.companyId
- [ ] Client-provided companyId never trusted
- [ ] Cross-tenant milestone access returns "not found" (no data leak)
- [ ] Proxy blocks unauthorized module access at middleware level

## 16. Docker Validation Checklist

- [ ] `docker-compose down --remove-orphans` completes
- [ ] `docker-compose build --no-cache` completes
- [ ] `docker-compose up -d` starts all services
- [ ] `http://localhost:3000` is accessible
- [ ] `/logistic` loads with dashboard
- [ ] `/logistic/orders` loads with progress columns
- [ ] `/logistic/orders/[id]` loads with milestone section
- [ ] Terminal state edit guard works in Docker
- [ ] BLOCKED resume behavior works in Docker
- [ ] Default milestone idempotency works in Docker