# PlantX v3.3.2 — Settings Shell Context + Module Purchase Flow Polish QA

**Version:** 3.3.2
**Date:** 2026-05-19

---

## 1. Shell Context Preservation

### 1.1 PlantLogistic → Plan & Usage preserves logistic shell

- [ ] Login as OEM admin with PlantLogistic entitlement
- [ ] Navigate to `/logistic`
- [ ] Click "Plan & Usage" in sidebar
- [ ] **Expected:** URL is `/logistic/settings/plan`
- [ ] **Expected:** Sidebar shows PlantLogistic nav items (Overview, Vehicle Orders, New Order, Plan & Usage)
- [ ] **Expected:** Header shows "PlantLogistic > [Company Name]"
- [ ] **Expected:** AppSwitcher shows PlantLogistic as "Active"
- [ ] **Expected:** Page content shows Plan & Usage (Current Plan, Module Access, Module Catalog, Usage, Features, Upgrade Requests)

### 1.2 PlantQuality → Plan & Usage preserves quality shell

- [ ] Login as OEM admin
- [ ] Navigate to `/quality/oem`
- [ ] Click "Plan & Usage" in sidebar
- [ ] **Expected:** URL is `/settings/plan`
- [ ] **Expected:** Sidebar shows PlantQuality OEM nav items (Dashboard, Defects, Field Quality, etc.)
- [ ] **Expected:** Header shows "PlantQuality > [Company Name]"
- [ ] **Expected:** AppSwitcher shows PlantQuality as "Active"
- [ ] **Expected:** Page content shows Plan & Usage (same platform-level content)

### 1.3 Direct URL access — logistic

- [ ] Navigate directly to `/logistic/settings/plan` as OEM admin
- [ ] **Expected:** Logistic sidebar/header context is shown
- [ ] **Expected:** No redirect to `/settings/plan`

### 1.4 Direct URL access — quality

- [ ] Navigate directly to `/settings/plan` as OEM admin
- [ ] **Expected:** Quality sidebar/header context is shown
- [ ] **Expected:** No redirect

### 1.5 Legacy redirects

- [ ] Navigate to `/oem/settings/plan`
- [ ] **Expected:** Redirects to `/settings/plan`
- [ ] Navigate to `/quality/oem/settings/plan`
- [ ] **Expected:** Redirects to `/settings/plan`

### 1.6 Auth redirect

- [ ] Visit `/settings/plan` without login
- [ ] **Expected:** Redirects to `/login`
- [ ] Visit `/logistic/settings/plan` as OEM admin
- [ ] **Expected:** Shows Plan & Usage with logistic sidebar

---

## 2. Module Catalog & Purchase Flow

### 2.1 Active modules

- [ ] As OEM admin with both modules entitled:
  - [ ] PlantQuality shows "Active" or "Live" badge
  - [ ] **Expected:** PlantQuality is NOT shown as "Soon"
  - [ ] PlantLogistic shows "Active" or "Live" badge
  - [ ] **Expected:** PlantLogistic is NOT shown as "Soon"

### 2.2 Locked modules

- [ ] As OEM admin WITHOUT PlantLogistic entitlement (e.g., `oem-free-company`):
  - [ ] PlantLogistic shows "Locked" badge
  - [ ] "Request access" button appears next to PlantLogistic
  - [ ] Click "Request access"
  - [ ] **Expected:** Inline form appears with optional message textarea
  - [ ] Click "Submit"
  - [ ] **Expected:** "Request submitted" confirmation appears (NOT a fake purchase success)
  - [ ] Click "Request access" again for same module
  - [ ] **Expected:** "Request already exists" message appears

### 2.3 Soon modules

- [ ] PlantDock, PlantQuote, PlantTrace, PlantAudit, PlantAsset, PlantFlow, PlantStaff all show "Soon" badge
- [ ] **Expected:** No "Request access" button on Soon modules
- [ ] **Expected:** No click action on Soon modules

### 2.4 Module catalog header

- [ ] "Available PlantX Modules" card shows "Online billing is not enabled yet" label
- [ ] **Expected:** Honest labeling, no fake purchase flow

### 2.5 Request access form behavior

- [ ] Clicking "Request access" on a locked module opens inline form
- [ ] Submitting with empty message still creates request
- [ ] Submitting with a message includes the message in the request
- [ ] Canceling the form returns to "Request access" button state
- [ ] **Expected:** No fake "Purchase successful" message at any point

---

## 3. Supplier Behavior

### 3.1 Supplier cannot access Plan & Usage

- [ ] Login as Supplier user
- [ ] **Expected:** No "Plan & Usage" link in sidebar
- [ ] Navigate to `/settings/plan` directly
- [ ] **Expected:** Redirected to `/login`

### 3.2 Supplier never sees PlantLogistic

- [ ] Supplier AppSwitcher only shows PlantQuality
- [ ] Supplier never sees "Request access" for PlantLogistic

---

## 4. PlantQuality Regression

### 4.1 Quality routes still work

- [ ] `/quality/oem` — Dashboard loads
- [ ] `/quality/oem/defects` — Defects list loads
- [ ] `/quality/oem/field` — Field Quality loads
- [ ] `/quality/oem/quality-intelligence` — Intelligence loads
- [ ] `/quality/oem/scorecard` — Scorecard loads
- [ ] `/quality/oem/notifications` — Notifications loads

### 4.2 Quality locked feature upgrade links

- [ ] Quality-intelligence page shows "Upgrade" link → clicking goes to `/settings/plan`
- [ ] Scorecard page shows "Upgrade" link → clicking goes to `/settings/plan`
- [ ] Executive cockpit shows "Upgrade" link → clicking goes to `/settings/plan`
- [ ] Related records panel shows "Upgrade" link → clicking goes to `/settings/plan`

### 4.3 Quality sidebar

- [ ] Plan & Usage link in Quality sidebar points to `/settings/plan`
- [ ] All Quality sidebar items still function correctly

---

## 5. PlantLogistic Regression

### 5.1 Logistic routes still work

- [ ] `/logistic` — Overview loads
- [ ] `/logistic/orders` — Vehicle Orders loads
- [ ] `/logistic/orders/new` — New Order loads

### 5.2 Logistic Plan & Usage

- [ ] `/logistic/settings/plan` — Plan & Usage loads with logistic sidebar
- [ ] Plan & Usage shows same content as quality route

---

## 6. Upgrade Requests

### 6.1 Existing upgrade request flow still works

- [ ] "Request Pro" and "Request Enterprise" buttons still appear
- [ ] Submitting a plan upgrade request creates an UpgradeRequest
- [ ] Upgrade request list shows historical requests

### 6.2 Module access request creates correct record

- [ ] Requesting module access creates UpgradeRequest with `sourceFeature: "MODULE_ACCESS:PLANT_LOGISTIC_MODULE"`
- [ ] The request appears in Upgrade Requests list on the page
- [ ] The message field includes `[Module Access Request: PLANT_LOGISTIC_MODULE]` prefix

---

## 7. Cross-module Navigation

### 7.1 AppSwitcher behavior unchanged

- [ ] From Quality Plan & Usage, AppSwitcher works correctly
- [ ] From Logistic Plan & Usage, AppSwitcher works correctly
- [ ] Switching modules from either Plan & Usage page navigates correctly

### 7.2 Sign out / Theme toggle

- [ ] Sign out button works from both Plan & Usage routes
- [ ] Theme toggle works from both Plan & Usage routes

---

## 8. Docker Validation

### 8.1 Build

- [ ] `docker compose build --no-cache` succeeds
- [ ] No build errors

### 8.2 Runtime

- [ ] `docker compose up -d` succeeds
- [ ] App is accessible at `http://localhost:3000`
- [ ] No fatal runtime errors in logs

### 8.3 Migration

- [ ] No new Prisma migration required (schema unchanged)
- [ ] `npx prisma validate` passes
- [ ] `npx prisma migrate status` is clean

---

## 9. Validation Commands

```bash
npm run lint
npm run typecheck
npm run build
npx prisma validate
npx prisma migrate status
```

```bash
docker compose down --remove-orphans
docker compose build --no-cache
docker compose up -d
docker compose ps
docker compose logs app --tail=100
```

---

## Summary Table

| Test Area | Expected | Status |
|-----------|----------|--------|
| Logistic sidebar preserved on Plan & Usage | Logistic sidebar shows | |
| Quality sidebar preserved on Plan & Usage | Quality sidebar shows | |
| `/logistic/settings/plan` route works | 200 OK, logistic shell | |
| `/settings/plan` route works | 200 OK, quality shell | |
| Legacy redirects work | `/oem/settings/plan` → `/settings/plan` | |
| Module catalog shows Live/Locked/Soon | Correct categorization | |
| Request access creates UpgradeRequest | Correct sourceFeature prefix | |
| No fake purchase success | Honest "Request submitted" only | |
| Supplier cannot access Plan & Usage | Redirect to /login | |
| PlantQuality routes unchanged | All quality routes work | |
| PlantLogistic routes unchanged | All logistic routes work | |
| AppSwitcher unchanged | Correct badge logic | |
| Sign out / Theme toggle | Works from both routes | |
| Docker build | Succeeds | |
| No fatal runtime errors | Clean logs | |